on:
  push:
    branshes:
      - master
           






jobs:
  build: 
    runs-on: ubuntu-latest

    steps: 
      - uses: actions/checkout@v3
      
      - name: "Login to Docker hub"
        run: docker login --username tahaelgamal111 --password dckr_pat_OTgSEmCx290h0MMIYICSPUJCuZM
